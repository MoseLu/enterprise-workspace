# ========== admin.bellis.com.cn ==========
# 管理应用：直接服务自己的 index.html，独立运行并加载 layout-app

server {
    listen       80;
    server_name  admin.bellis.com.cn;
    return 301 https://$host$request_uri;
}

server {
    listen       443 ssl;
    server_name  admin.bellis.com.cn;

    # SSL 证书
    ssl_certificate      /home/ssl/bellis.com.cn_nginx/bellis.com.cn_bundle.pem;
    ssl_certificate_key  /home/ssl/bellis.com.cn_nginx/bellis.com.cn.key;
    ssl_session_cache    shared:SSL:10m;
    ssl_session_timeout  10m;
    ssl_session_tickets  off;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers  HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers  on;

    # 允许请求大小
    client_max_body_size 10M;

    # Gzip 压缩配置（关键：减少传输大小，提升加载速度）
    gzip on;
    gzip_vary on;  # 关键：根据 Accept-Encoding 头变化响应，避免 gzip 缓存问题
    gzip_min_length 1024;  # 只压缩大于1KB的文件
    gzip_proxied any;
    gzip_comp_level 6;     # 压缩级别（1-9，6是性能和压缩率的平衡）
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml
        application/xml+rss
        application/atom+xml
        image/svg+xml
        font/woff
        font/woff2
        font/ttf
        font/otf;
    gzip_disable "msie6";   # 禁用IE6的gzip（IE6不支持）
    
    # 关键：禁用 gzip 静态文件缓存，确保新构建产物能被立即访问
    # 这样可以避免 gzip 缓存导致访问旧文件的问题
    gzip_static off;  # 禁用预压缩文件（.gz），强制实时压缩，确保访问最新文件

    # 关键：admin 应用兼容两种部署方式：
    # 1) 直铺部署：/www/wwwroot/admin.bellis.com.cn/
    # 2) releases/current：/www/wwwroot/admin.bellis.com.cn/current/
    # 所以下面所有 try_files 都优先尝试 /current，再回退到根目录
    root /www/wwwroot/admin.bellis.com.cn;
    index index.html;

    # CORS 配置
    set $cors_origin "";
    if ($http_origin ~* ^https?://.*\.bellis\.com\.cn$) {
        set $cors_origin $http_origin;
    }

    # API 代理（必须在静态文件之前）
    location /api/ {
        proxy_pass http://10.0.0.168:8115/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        proxy_set_header X-Forwarded-Prefix /api;
        proxy_http_version 1.1;
        proxy_set_header X-Original-URI $request_uri;
        proxy_set_header X-Original-Method $request_method;
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        proxy_busy_buffers_size 8k;
        proxy_cookie_path / /;
        
        # 缓存配置 - 1小时
        expires 1h;
        add_header Cache-Control "public, max-age=3600" always;

        add_header Access-Control-Allow-Origin $cors_origin always;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS, PATCH" always;
        add_header Access-Control-Allow-Headers "Authorization, Content-Type, X-Requested-With, Accept, Origin, X-Tenant-Id" always;
        add_header Access-Control-Allow-Credentials true always;

        # 处理 OPTIONS 预检请求
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin $cors_origin;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS, PATCH";
            add_header Access-Control-Allow-Headers "Authorization, Content-Type, X-Requested-With, Accept, Origin, X-Tenant-Id";
            add_header Access-Control-Max-Age 1728000;
            add_header Content-Type 'text/plain charset=UTF-8';
            add_header Content-Length 0;
            return 204;
        }
    }

    # 注意：子应用现在直接通过子域名根路径访问，不再使用 /micro-apps/ 路径

    # bridge.html 特殊配置：代理到主域的 bridge.html（用于跨子域通信）
    # 必须在 /index.html 之前，确保精确匹配
    # 注意：location = 是精确匹配，优先级最高，不会被其他 location 覆盖
    location = /bridge.html {
        # 代理到主域的 bridge.html
        proxy_pass https://bellis.com.cn/bridge.html;
        proxy_set_header Host bellis.com.cn;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_server_name on;
        
        # 缓存控制：不缓存 bridge.html，确保总是获取最新版本
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        
        # 关键：允许被所有 bellis.com.cn 子域嵌入
        add_header Content-Security-Policy "frame-ancestors 'self' https://*.bellis.com.cn https://bellis.com.cn" always;
        
        # 其他安全头
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        
        # 确保正确的 Content-Type
        proxy_set_header Accept "text/html";
        default_type text/html;
    }
    
    # layout-app 的静态资源（优先匹配，确保 layout-app 的资源从正确的目录加载）
    # 关键：layout-app 的资源在 layout.bellis.com.cn 的 /assets/ 目录下
    # 注意：这个规则必须在 admin-app 的 /assets/ 规则之前，确保优先匹配
    location /assets/layout/ {
        # 关键：处理不带 hash 的图片路径（如 /assets/layout/light.png）
        # 重写为带 hash 的路径（如 /assets/layout/light-*.png）
        # 使用 try_files 尝试匹配带 hash 的文件名
        # 注意：由于文件名包含动态 hash，这里使用通配符匹配
        # 如果请求的是 /assets/layout/light.png，尝试匹配 /assets/layout/light-*.png
        
        # 先尝试直接代理（如果路径已经包含 hash）
        set $layout_path $request_uri;
        
        # 如果路径不包含 hash，尝试匹配带 hash 的文件名
        # 注意：Nginx 无法直接匹配通配符，所以这里先代理，让 layout-app 处理 404
        # 或者使用 rewrite 规则，但这需要知道所有可能的文件名
        
        # 代理到 layout.bellis.com.cn 的 /assets/layout/ 路径（layout-app 的资源在 /assets/layout/ 目录下）
        proxy_pass https://layout.bellis.com.cn/assets/layout/;
        proxy_set_header Host layout.bellis.com.cn;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_server_name on;
        
        # 关键：禁用代理缓存，确保每次都从上游服务器获取最新资源
        proxy_cache off;
        proxy_no_cache 1;
        proxy_cache_bypass 1;
        # 清除可能影响缓存的请求头
        proxy_set_header If-None-Match "";
        proxy_set_header If-Modified-Since "";
        
        # 关键：确保上游返回 404 时，直接返回 404，不进行任何回退
        proxy_intercept_errors off;
        
        # 调试：添加缓存状态响应头
        add_header X-Proxy-Cache-Status "DISABLED" always;
        add_header X-Proxy-Cache-Off "true" always;
        add_header X-Proxy-No-Cache "1" always;
        add_header X-Proxy-Cache-Bypass "1" always;
        add_header X-Upstream-URL "$upstream_addr" always;
        add_header X-Request-URI "$request_uri" always;
        add_header X-Proxy-Pass-URL "https://layout.bellis.com.cn/assets/layout/" always;
        
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        # 关键：允许所有子域名跨域访问（包括 admin.bellis.com.cn）
        add_header Access-Control-Allow-Origin "*" always;
        add_header Access-Control-Allow-Credentials true always;
        add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type" always;
        access_log off;
        
        # 处理 OPTIONS 预检请求
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin "*" always;
            add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type" always;
            add_header Access-Control-Max-Age 1728000;
            add_header Content-Type 'text/plain charset=UTF-8';
            add_header Content-Length 0;
            return 204;
        }
        
        # 确保正确的 MIME 类型
        types {
            application/javascript js mjs;
            text/css css;
            image/svg+xml svg;
            image/png png;
            image/jpeg jpg jpeg;
            image/gif gif;
            image/webp webp;
            application/json json;
            application/manifest+json webmanifest;
            font/woff woff;
            font/woff2 woff2;
            font/ttf ttf;
            font/otf otf;
        }
        default_type application/javascript;
    }

    # HTML文件不缓存（必须在其他location之前，精确匹配）
    location = /index.html {
        root /www/wwwroot/admin.bellis.com.cn;
        try_files /current/index.html /index.html =404;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        
        add_header Access-Control-Allow-Origin $cors_origin always;

        # 安全头
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    }

    # admin应用的静态资源（当从子域名直接访问时，/assets/ 应该指向admin应用的资源）
    # 注意：layout-app 的资源在 /assets/layout/ 中，不会与 admin-app 的资源冲突
    # nginx 匹配规则：更长的前缀匹配优先，所以 /assets/layout/ 会优先于 /assets/
    # 带hash的文件名可以长期缓存（Vite构建会生成hash）
    #
    # 关键兜底：旧 index.html 可能缓存了 /assets/index-xxx.js（hash 入口文件）。
    # 我们将入口文件改为稳定的 /assets/index.js，并在 Nginx 做回退：
    # - /assets/index-xxx.js -> try_files $uri /assets/index.js
    # 这样即使客户端持有旧引用，也不会 404。
    location ~ ^/assets/index-[^/]+\.js$ {
        root /www/wwwroot/admin.bellis.com.cn;
        try_files /current$uri /current/assets/index.js $uri /assets/index.js =404;
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        add_header Access-Control-Allow-Origin "*" always;
        add_header Access-Control-Allow-Credentials true always;
        add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type" always;
        access_log off;

        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin "*" always;
            add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type" always;
            add_header Access-Control-Max-Age 1728000;
            add_header Content-Type 'text/plain charset=UTF-8';
            add_header Content-Length 0;
            return 204;
        }
    }

    location = /assets/index.js {
        root /www/wwwroot/admin.bellis.com.cn;
        try_files /current/assets/index.js /assets/index.js =404;
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        add_header Access-Control-Allow-Origin "*" always;
        add_header Access-Control-Allow-Credentials true always;
        add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type" always;
        access_log off;
    }

    # admin应用的静态资源（从本地文件系统提供）
    # 带hash的文件名可以长期缓存（Vite构建会生成hash）
    location /assets/ {
        root /www/wwwroot/admin.bellis.com.cn;
        try_files /current$uri $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        # 关键：允许所有域名访问（包括当前域名和跨域）
        add_header Access-Control-Allow-Origin "*" always;
        add_header Access-Control-Allow-Credentials true always;
        add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type" always;
        access_log off;
        
        # 处理 OPTIONS 预检请求
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin "*" always;
            add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type" always;
            add_header Access-Control-Max-Age 1728000;
            add_header Content-Type 'text/plain charset=UTF-8';
            add_header Content-Length 0;
            return 204;
        }
        
        # 确保正确的 MIME 类型
        types {
            application/javascript js mjs;
            text/css css;
            image/svg+xml svg;
            image/png png;
            image/jpeg jpg jpeg;
            image/gif gif;
            image/webp webp;
            application/json json;
            application/manifest+json webmanifest;
            font/woff woff;
            font/woff2 woff2;
            font/ttf ttf;
            font/otf otf;
        }
        default_type application/octet-stream;
    }

    # admin应用的静态资源：icons、favicon等（从admin应用自己的目录加载）
    # admin-app 有自己的 public 目录，包含 icons 和 templates
    # 带hash的文件名可以长期缓存（Vite构建会生成hash）
    location /icons/ {
        root /www/wwwroot/admin.bellis.com.cn;
        try_files /current$uri $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        add_header Access-Control-Allow-Origin "*" always;
        add_header Access-Control-Allow-Credentials true always;
        access_log off;
        
        # 确保正确的 MIME 类型
        types {
            image/png png;
            image/jpeg jpg jpeg;
            image/svg+xml svg;
            image/gif gif;
            image/webp webp;
            image/x-icon ico;
            application/json json;
            application/manifest+json webmanifest;
        }
        default_type image/png;
    }

    # 匹配根目录下的 CSS 文件（某些构建配置可能将 CSS 文件输出到根目录）
    # 必须在 SPA 路由之前，确保 CSS 文件不被路由捕获
    # 关键：排除 /assets/ 路径，因为 /assets/ 应该由上面的 location /assets/ 处理
    # 如果根目录不存在，尝试在 /assets/ 目录查找（因为实际文件可能在 assets 目录）
    location ~* ^/(?!assets/)([^/]+\.css)$ {
        root /www/wwwroot/admin.bellis.com.cn;
        # 先尝试 current 根目录/当前根目录，再尝试 current/assets 与根目录 assets
        try_files /current$uri $uri /current/assets/$1 /assets/$1 =404;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        add_header Content-Type "text/css; charset=utf-8" always;
        add_header Access-Control-Allow-Origin "https://bellis.com.cn" always;
        add_header Access-Control-Allow-Credentials true always;
        access_log off;
    }

    # 匹配根目录下的 JS/MJS 文件（某些构建配置可能将 JS 文件输出到根目录）
    # 必须在 SPA 路由之前，确保 JS 文件不被路由捕获
    # 关键：排除 /assets/ 路径，因为 /assets/ 应该由上面的 location /assets/ 处理
    # 如果根目录不存在，尝试在 /assets/ 目录查找（因为实际文件可能在 assets 目录）
    location ~* ^/(?!assets/)([^/]+\.(js|mjs))$ {
        root /www/wwwroot/admin.bellis.com.cn;
        # 先尝试 current 根目录/当前根目录，再尝试 current/assets 与根目录 assets
        try_files /current$uri $uri /current/assets/$1 /assets/$1 =404;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        add_header Content-Type "application/javascript; charset=utf-8" always;
        add_header Access-Control-Allow-Origin "https://bellis.com.cn" always;
        add_header Access-Control-Allow-Credentials true always;
        access_log off;
    }

    # 阻止源码路径被SPA路由捕获（返回404而不是HTML）
    # 这些路径不应该在生产环境出现，如果出现说明构建有问题
    location ~ ^/packages/ {
        return 404;
        access_log off;
    }

    location ~ ^/src/ {
        return 404;
        access_log off;
    }

    # 匹配其他静态资源文件扩展名（必须在 SPA 路由之前）
    # 确保所有静态资源文件都有明确的处理规则，不会被 SPA 路由捕获
    location ~* \.(jpg|jpeg|png|gif|ico|svg|webp|woff|woff2|ttf|eot|otf|json|webmanifest|xml|txt|pdf)$ {
        root /www/wwwroot/admin.bellis.com.cn;
        try_files /current$uri $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        add_header Access-Control-Allow-Origin "*" always;
        add_header Access-Control-Allow-Credentials true always;
        access_log off;
    }

    # 健康检查（必须在 SPA 路由之前）
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # HTML 文件必须禁用缓存，确保每次获取最新的 index.html
    # 关键：排除 /api/ 路径，避免 API 请求被误判为静态资源
    location ~* ^(?!/api/).*\.html$ {
        root /www/wwwroot/admin.bellis.com.cn;
        try_files /current$uri $uri =404;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        add_header Access-Control-Allow-Origin "https://bellis.com.cn" always;
        add_header Access-Control-Allow-Credentials true always;
        
        # 安全头
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        access_log off;
    }

    # SPA 路由支持 - 直接返回admin应用的 index.html（独立运行模式，通过代码加载 layout-app）
    location = / {
        root /www/wwwroot/admin.bellis.com.cn;
        try_files /current/index.html /index.html =404;
        
        # HTML文件不缓存
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        
        add_header Access-Control-Allow-Origin $cors_origin always;

        # 安全头
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    }
    
    # 处理所有其他路径（SPA 路由）- 返回admin应用的 index.html
    # 注意：这个规则会匹配所有未被前面规则匹配的路径
    # 确保前面的静态资源规则已经处理了所有静态文件
    location / {
        root /www/wwwroot/admin.bellis.com.cn;
        # 关键：兼容 current + 直铺；且只尝试文件（不尝试目录）
        try_files /current$uri $uri /current/index.html /index.html;
        
        # HTML文件不缓存
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        
        add_header Access-Control-Allow-Origin $cors_origin always;

        # 安全头
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    }
}

