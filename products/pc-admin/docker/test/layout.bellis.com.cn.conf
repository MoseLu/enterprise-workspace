# ========== layout.test.bellis.com.cn ==========
# 布局微应用：为所有子应用提供统一的布局组件

# CORS 配置：使用 map 指令动态设置允许的 Origin
# 关键：map 指令必须在 server 块之外定义
# 关键：匹配所有 test.bellis.com.cn 子域名（包括主域名）
map $http_origin $cors_origin {
    default "";
    # 匹配所有 test.bellis.com.cn 子域名（如 admin.test.bellis.com.cn, logistics.test.bellis.com.cn 等）
    # 也匹配主域名 test.bellis.com.cn
    # 关键：使用更宽松的匹配，确保所有子域名都能匹配
    ~^https?://([^/]+\.)?test\\.bellis\\.com\\.cn(:[0-9]+)?$ $http_origin;
}

# 关键：创建一个后备的 CORS origin，如果 $cors_origin 为空，使用 $http_origin 或构建完整的 Origin
# 这个 map 用于确保总是有一个有效的 CORS origin
# 关键：优先使用 $cors_origin，如果为空则使用 $http_origin，如果仍然为空则使用当前请求的协议和主机
map "$cors_origin" $final_cors_origin {
    default $cors_origin;
    "" $http_origin;
}

# 如果 $final_cors_origin 仍然为空，使用当前请求的协议和主机构建 Origin
map "$final_cors_origin:$scheme:$http_host" $final_cors_origin_fallback {
    default $final_cors_origin;
    ~^:(https?):(.+)$ $1://$2;
}

server {
    listen       80;
    server_name  layout.test.bellis.com.cn;
    return 301 https://$host$request_uri;
}

server {
    listen       443 ssl;
    server_name  layout.test.bellis.com.cn;

    ssl_certificate      /home/ssl/bellis.com.cn_nginx/bellis.com.cn_bundle.pem;
    ssl_certificate_key  /home/ssl/bellis.com.cn_nginx/bellis.com.cn.key;
    ssl_session_cache    shared:SSL:10m;
    ssl_session_timeout  10m;
    ssl_session_tickets  off;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers  HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers  on;
    client_max_body_size 10M;

    # Gzip 压缩配置（关键：减少传输大小，提升加载速度）
    gzip on;
    gzip_vary on;  # 关键：根据 Accept-Encoding 头变化响应，避免 gzip 缓存问题
    gzip_min_length 1024;  # 只压缩大于1KB的文件
    gzip_proxied any;
    gzip_comp_level 6;     # 压缩级别（1-9，6是性能和压缩率的平衡）
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml
        application/xml+rss
        application/atom+xml
        image/svg+xml
        font/woff
        font/woff2
        font/ttf
        font/otf;
    gzip_disable "msie6";   # 禁用IE6的gzip（IE6不支持）
    
    # 关键：禁用 gzip 静态文件缓存，确保新构建产物能被立即访问
    # 这样可以避免 gzip 缓存导致访问旧文件的问题
    gzip_static off;  # 禁用预压缩文件（.gz），强制实时压缩，确保访问最新文件

    # 静态文件根目录（兼容直铺 + releases/current）
    root /www/test/layout.test.bellis.com.cn;
    index index.html;

    # API 代理（必须在静态文件之前）
    location /api/ {
        proxy_pass http://10.0.0.168:8115/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        proxy_set_header X-Forwarded-Prefix /api;
        proxy_http_version 1.1;
        proxy_set_header X-Original-URI $request_uri;
        proxy_set_header X-Original-Method $request_method;
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        proxy_busy_buffers_size 8k;
        proxy_cookie_path / /;
        
        # 缓存配置 - 1小时
        expires 1h;
        add_header Cache-Control "public, max-age=3600" always;

        add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS, PATCH" always;
        add_header Access-Control-Allow-Headers "Authorization, Content-Type, X-Requested-With, Accept, Origin, X-Tenant-Id" always;
        add_header Access-Control-Allow-Credentials true always;

        # 处理 OPTIONS 预检请求
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS, PATCH" always;
            add_header Access-Control-Allow-Headers "Authorization, Content-Type, X-Requested-With, Accept, Origin, X-Tenant-Id" always;
            add_header Access-Control-Allow-Credentials true always;
            add_header Access-Control-Max-Age 1728000 always;
            add_header Content-Type 'text/plain charset=UTF-8' always;
            add_header Content-Length 0 always;
            return 204;
        }
    }

    # manifest.json 文件（必须在其他location之前，精确匹配）
    # 关键：允许所有子域名访问 manifest.json，用于共享资源加载
    location = /manifest.json {
        root /www/test/layout.test.bellis.com.cn;
        try_files /current/manifest.json /manifest.json =404;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        add_header Content-Type "application/json; charset=utf-8" always;
        # 关键：允许所有 test.bellis.com.cn 子域名访问
        add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
        add_header Access-Control-Allow-Credentials true always;
        add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type" always;
        access_log off;
        
        # 处理 OPTIONS 预检请求
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
            add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type" always;
            add_header Access-Control-Max-Age 1728000;
            add_header Content-Type 'text/plain charset=UTF-8';
            add_header Content-Length 0;
            return 204;
        }
    }

    # HTML文件不缓存（必须在其他location之前，精确匹配）
    location = /index.html {
        root /www/test/layout.test.bellis.com.cn;
        try_files /current/index.html /index.html =404;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        
        add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
        
        # 安全头
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    }

    # layout-app 的静态资源目录（优先匹配，必须在 SPA 路由之前）
    # 关键：layout-app 的资源在 /assets/layout/ 目录下，与子应用的 /assets/ 区分开
    # 带hash的文件名可以长期缓存（Vite构建会生成hash）
    location ^~ /assets/layout/ {
        root /www/test/layout.test.bellis.com.cn;
        try_files /current$uri $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        # 关键：允许所有 test.bellis.com.cn 子域名访问共享资源
        # 使用 $final_cors_origin（通过 map 指令确保总是有有效值）
        add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
        add_header Access-Control-Allow-Credentials true always;
        add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type" always;
        # 关键：添加 Vary 头，确保 CORS 和 gzip 缓存正确工作
        # 关键：Vary: Origin 确保不同 Origin 的响应被正确缓存，避免 CORS 头部被缓存
        add_header Vary "Origin, Accept-Encoding" always;
        # 关键：禁用 ETag，避免缓存问题（文件名已包含 hash，不需要 ETag）
        etag off;
        access_log off;
        
        # 处理 OPTIONS 预检请求
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
            add_header Access-Control-Allow-Credentials true always;
            add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type" always;
            add_header Access-Control-Max-Age 1728000;
            add_header Content-Type 'text/plain charset=UTF-8';
            add_header Content-Length 0;
            return 204;
        }
        
        # 确保正确的 MIME 类型
        types {
            application/javascript js mjs;
            text/css css;
            image/svg+xml svg;
            image/png png;
            image/jpeg jpg jpeg;
            image/gif gif;
            image/webp webp;
            application/json json;
            application/manifest+json webmanifest;
            font/woff woff;
            font/woff2 woff2;
            font/ttf ttf;
            font/otf otf;
        }
        default_type application/javascript;
    }

    # 回退规则：将错误的 /assets/xxx.js 路径重写为 /assets/layout/xxx.js
    # 关键：由于 /assets/layout/ 是更长的前缀，它会优先匹配
    # 所以这个 location /assets/ 只会匹配 /assets/xxx.js（不包括 /assets/layout/xxx.js）
    # 这个规则用于处理某些情况下生成的路径缺少 /layout/ 部分的问题
    #
    # 关键兜底：旧引用可能请求 /assets/index-xxx.js（hash 入口）。
    # layout-app 入口文件改为稳定的 /assets/layout/index.js，并在这里做回退，避免 404。
    location ~ ^/assets/index-[^/]+\.js$ {
        root /www/test/layout.test.bellis.com.cn;
        # 关键：不要命中“旧的 hash 入口文件”（即使它存在），否则它内部的相对 import 会去请求 /assets/*.js，导致 404。
        # 这里强制回退到稳定入口 /assets/layout/index.js，让后续 chunk 按 /assets/layout/* 正确解析。
        try_files /current/assets/layout/index.js /assets/layout/index.js =404;
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
        add_header Access-Control-Allow-Credentials true always;
        add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type" always;
        add_header Vary "Accept-Encoding" always;
        etag off;
        access_log off;

        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
            add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type" always;
            add_header Access-Control-Max-Age 1728000;
            add_header Content-Type 'text/plain charset=UTF-8';
            add_header Content-Length 0;
            return 204;
        }
    }

    # 关键兜底：某些 loader/历史版本会请求 /assets/index.js（缺少 /layout/ 前缀）
    # 注意：必须用精确匹配，避免被后面的静态资源正则 location 抢先匹配而直接 404
    location = /assets/index.js {
        root /www/test/layout.test.bellis.com.cn;
        try_files /current/assets/index.js /current/assets/layout/index.js /assets/index.js /assets/layout/index.js =404;
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
        add_header Access-Control-Allow-Credentials true always;
        add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type" always;
        add_header Vary "Accept-Encoding" always;
        etag off;
        access_log off;

        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
            add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type" always;
            add_header Access-Control-Max-Age 1728000;
            add_header Content-Type 'text/plain charset=UTF-8';
            add_header Content-Length 0;
            return 204;
        }
    }

    location = /assets/layout/index.js {
        root /www/test/layout.test.bellis.com.cn;
        try_files /current/assets/layout/index.js /assets/layout/index.js =404;
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
        add_header Access-Control-Allow-Credentials true always;
        add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type" always;
        add_header Vary "Accept-Encoding" always;
        etag off;
        access_log off;
    }

    # 回退规则：将错误的 /assets/xxx.js 路径重写为 /assets/layout/xxx.js
    # 关键：由于 /assets/layout/ 是更长的前缀，它会优先匹配
    # 所以这个 location /assets/ 只会匹配 /assets/xxx.js（不包括 /assets/layout/xxx.js）
    # layout应用的静态资源（从本地文件系统提供）
    # 注意：请求是 /assets/xxx.js，实际文件在 /assets/layout/xxx.js
    # 带hash的文件名可以长期缓存（Vite构建会生成hash）
    location ^~ /assets/ {
        root /www/test/layout.test.bellis.com.cn;
        # 将请求重写为 /assets/layout/ 路径
        # 使用 break 避免再次匹配 location
        rewrite ^/assets/(.+)$ /assets/layout/$1 break;
        # 尝试查找文件
        try_files /current$uri $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        # 关键：允许所有 test.bellis.com.cn 子域名访问共享资源
        add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
        add_header Access-Control-Allow-Credentials true always;
        add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type" always;
        add_header Vary "Accept-Encoding" always;
        etag off;
        access_log off;
        
        # 处理 OPTIONS 预检请求
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
            add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type" always;
            add_header Access-Control-Max-Age 1728000;
            add_header Content-Type 'text/plain charset=UTF-8';
            add_header Content-Length 0;
            return 204;
        }
        
        # 确保正确的 MIME 类型
        types {
            application/javascript js mjs;
            text/css css;
            image/svg+xml svg;
            image/png png;
            image/jpeg jpg jpeg;
            image/gif gif;
            image/webp webp;
            application/json json;
            application/manifest+json webmanifest;
            font/woff woff;
            font/woff2 woff2;
            font/ttf ttf;
            font/otf otf;
        }
        default_type application/javascript;
    }

    location /icons/ {
        root /www/test/layout.test.bellis.com.cn;
        try_files /current$uri $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
        access_log off;
    }

    # 静态资源文件扩展名匹配（优先匹配）
    # 带hash的文件名可以长期缓存（Vite构建会生成hash）
    # 关键：排除 /api/ 路径，避免 API 请求被误判为静态资源
    location ~* ^(?!/api/).*\.(js|mjs|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|webp|json|webmanifest)$ {
        root /www/test/layout.test.bellis.com.cn;
        try_files /current$uri $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
        access_log off;
    }

    # SPA 路由支持（放在最后，作为兜底）
    # 关键：只尝试文件，不尝试目录，避免将路由路径误认为文件系统路径
    location / {
        root /www/test/layout.test.bellis.com.cn;
        # 只尝试文件，如果文件不存在直接返回 index.html（不尝试目录）
        try_files /current$uri $uri /current/index.html /index.html;
        
        # HTML文件不缓存
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        
        add_header Access-Control-Allow-Origin $final_cors_origin_fallback always;
        
        # 安全头
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    }

    # 健康检查
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}

