/**
 * 路由配置模块
 * 负责配置Vue Router
 */
import type { App } from 'vue';
import router from '../../router';
/**
 * 配置路由
 */
export declare const setupRouter: (app: App) => void;
/**
 * 导出路由实例
 */
export { router };
