/**
 * 设置面板相关枚举定义
 */

// 菜单类型
export enum MenuTypeEnum {
  LEFT = 'left',
  TOP = 'top',
  TOP_LEFT = 'top-left',
  DUAL_MENU = 'dual-menu',
}

// 系统主题类型
export enum SystemThemeEnum {
  DARK = 'dark',
  LIGHT = 'light',
  AUTO = 'auto',
}

// 菜单主题类型
export enum MenuThemeEnum {
  DARK = 'dark',
  LIGHT = 'light',
  DESIGN = 'design',
}

// 容器宽度类型
export enum ContainerWidthEnum {
  FULL = '100%',
  BOXED = '1200px',
}

// 盒子样式类型
export enum BoxStyleType {
  BORDER = 'border-mode',
  SHADOW = 'shadow-mode',
}

// 全局风格套件
export enum StylePresetEnum {
  MINIMAL = 'minimal',
  GLASS = 'glass',
  CYBER = 'cyber',
}

