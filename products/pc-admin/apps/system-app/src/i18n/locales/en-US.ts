/**
 * system-app English business messages
 * 注意：所有翻译现在都在 config.ts 中，通过 getLocaleMessages() 获取
 */
import type { SystemAppLocaleMessages } from '../types';

export const enUS: SystemAppLocaleMessages = {} as SystemAppLocaleMessages;

