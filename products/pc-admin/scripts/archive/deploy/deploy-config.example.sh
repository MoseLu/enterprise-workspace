#!/bin/bash

# BTC ShopFlow 部署配置示例
# 复制此文件为 deploy-config.sh 并填写实际配置
# 然后在运行 bps-all.sh 前执行: source scripts/deploy-config.sh

# 服务器地址
export SERVER_HOST=47.112.31.96

# 服务器用户名
export SERVER_USER=root

# 服务器密码
export SERVER_PASSWORD=Bellis99+

# SSH 端口（默认 22）
export SERVER_PORT=22

# 远程部署基础路径
export REMOTE_BASE_PATH=/www/wwwroot

