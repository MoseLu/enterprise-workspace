# 工作区根目录（命名建议：业务域+平台，如xxx-business-platform）
project-workspace/
├── README.md                          # 工作区总入口：项目概述、环境搭建、CRUD自动生成流程指引
├── CHANGELOG.md                       # 版本变更日志：记录每次发布的功能、修复、兼容说明（含CRUD生成记录）
├── package.json                       # 工作区级依赖：Turbo repo根配置、跨端/跨环境脚本
├── turbo.json                         # Turbo全局配置：前端monorepo任务调度（多端构建/测试）
├── docker-compose.yml                 # 本地开发中间件：一键启动nacos/Redis/Keycloak/Mysql（开发环境）
├── docker-compose-ci.yml              # CI专用中间件：Jenkins/GitHub Actions运行时临时中间件（测试环境）
├── docker-compose-ai-dev.yml          # AI-Studio本地开发：数据库/代码生成/热更新依赖中间件（新增）
├── .env                               # 本地开发环境变量：中间件地址、环境标识（加入.gitignore）
├── .env.example                       # 环境变量示例：提交到Git，含CRUD生成相关配置（无敏感信息）
├── .gitignore                         # 全局忽略规则：logs、node_modules、target、.env、artifacts、生成的临时代码等
├── .editorconfig                      # 编辑器统一配置：缩进、编码、换行符（跨IDE开发规范）
├── .eslintrc.js                       # 全局ESLint配置：前端/Node脚本代码规范（子项目继承）
├── .prettierrc                        # 全局Prettier配置：代码格式化规则（跨技术栈统一）
├── LICENSE                            # 项目许可证（开源/闭源声明）
│
# -------------------------- 【全局通用脚本】：开发/CI/CD通用操作 --------------------------
├── scripts/
│   ├── dev/                           # 开发阶段脚本
│   │   ├── init-env.sh                # 环境初始化：安装所有依赖、配置中间件、初始化数据库
│   │   ├── start-all.sh               # 一键启动：前端+后端+AI Studio+辅助项目（本地开发）
│   │   ├── start-frontend.sh          # 仅启动Taro多端：Turbo dev模式（多端热更）
│   │   ├── start-pc-admin.sh          # 仅启动PC中后台：Ant Design Pro开发模式
│   │   ├── start-backend.sh           # 仅启动业务后端：SpringBoot开发模式（热部署）
│   │   ├── start-ai-studio.sh         # 仅启动AI Studio基座：本地开发调试CRUD生成流程
│   │   └── check-deps.sh              # 依赖检查：校验各技术栈依赖版本是否符合规范
│   ├── ci/                            # CI阶段通用脚本（GitHub Actions/Jenkins共用）
│   │   ├── pre-validate.sh            # CI前置校验：代码规范、依赖完整性、配置合法性
│   │   ├── cache-deps.sh              # 依赖缓存：前端/node_modules、后端/maven、Python/pip、Go/mod
│   │   ├── artifact-check.sh          # 产物校验：检查构建包完整性、版本号合法性
│   │   └── crud-code-validate.sh      # CRUD生成代码校验：符合项目编码规范（新增）
│   ├── cd/                            # CD阶段通用脚本（GitHub Actions/Jenkins共用）
│   │   ├── deploy-test.sh             # 测试环境部署：推送镜像、执行部署、健康检查
│   │   ├── deploy-prod.sh             # 生产环境部署：需手动确认、灰度发布、发布后验证
│   │   ├── deploy-ai-studio.sh        # AI Studio基座专属部署脚本（测试/生产）
│   │   ├── rollback.sh                # 回滚脚本：部署失败时回滚到上一版本（镜像/配置/数据）
│   │   └── hot-update-prod.sh         # 生产环境热更新脚本：CRUD生成代码热更新（新增核心）
│   └── release/                       # 发布阶段脚本
│       ├── create-tag.sh              # 创建版本标签：自动生成Git Tag（如v1.0.0）
│       ├── generate-changelog.sh      # 生成变更日志：从Git提交记录自动生成CHANGELOG.md
│       └── crud-release-record.sh     # CRUD生成代码发布记录：记录表名、生成版本、部署状态（新增）
│
# -------------------------- 【CI/CD核心配置】：同时适配GitHub Actions + Jenkins --------------------------
├── .cicd/
│   ├── github-actions/                # GitHub Actions专属配置（标准目录：.github/workflows也保留）
│   │   ├── templates/                 # 复用模板：多技术栈构建/测试通用步骤
│   │   │   ├── frontend-build.yml     # Taro多端构建模板
│   │   │   ├── pc-admin-build.yml     # PC中后台构建模板
│   │   │   ├── backend-build.yml      # 业务后端构建模板
│   │   │   ├── ai-studio-build.yml    # AI Studio基座构建模板
│   │   │   ├── auxiliary-build.yml    # 辅助项目构建模板
│   │   │   └── crud-code-gen.yml      # CRUD代码生成+数据库建表模板（新增核心）
│   │   ├── ci.yml                     # GitHub Actions CI流程：代码检查、构建、单元测试
│   │   ├── cd-test.yml                # GitHub Actions CD流程：部署到测试环境
│   │   ├── cd-prod.yml                # GitHub Actions CD流程：部署到生产环境（手动触发）
│   │   ├── cd-ai-studio.yml           # AI Studio基座专属CD流程
│   │   └── crud-hot-update.yml        # CRUD生成代码热更新流水线（测试→生产，新增核心）
│   ├── jenkins/                       # Jenkins专属配置
│   │   ├── Jenkinsfile.ci             # Jenkins CI流水线：复用ci/脚本，适配Jenkins语法
│   │   ├── Jenkinsfile.cd-test        # Jenkins CD流水线：部署测试环境
│   │   ├── Jenkinsfile.cd-prod        # Jenkins CD流水线：部署生产环境（需审批）
│   │   ├── Jenkinsfile.cd-ai-studio   # AI Studio基座专属Jenkins流水线
│   │   ├── Jenkinsfile.crud-code-gen  # CRUD代码生成+审核+部署流水线（新增核心）
│   │   ├── pipelines/                 # Jenkins流水线片段：拆分通用步骤（如构建、推送镜像）
│   │   │   └── crud-hot-update.groovy # CRUD热更新流水线片段：代码替换、数据库执行、验证（新增）
│   │   └── scripts/                   # Jenkins专属脚本：适配Jenkins agent环境
│   ├── env/                           # CI/CD环境配置（非敏感）
│   │   ├── test.env                   # 测试环境配置：中间件地址、镜像仓库、部署路径
│   │   ├── prod.env                   # 生产环境配置：非敏感参数（敏感参数走CI/Jenkins密钥）
│   │   ├── ai-studio-test.env         # AI Studio测试环境配置
│   │   ├── ai-studio-prod.env         # AI Studio生产环境配置
│   │   ├── crud-test.env              # CRUD生成测试环境配置：数据库地址、代码生成路径（新增）
│   │   ├── crud-prod.env              # CRUD生成生产环境配置：热更新路径、数据库权限（新增）
│   │   └── common.env                 # 通用配置：镜像标签规则、构建路径、超时时间
│   └── artifacts/                     # CI/CD产物临时目录（加入.gitignore）
│       ├── frontend/                  # Taro多端构建产物
│       ├── pc-admin/                  # PC中后台构建产物
│       ├── backend/                   # 业务后端Jar包/镜像
│       ├── ai-studio/                 # AI Studio基座构建产物
│       ├── auxiliary/                 # Python/Go辅助项目构建产物
│       └── crud-generated/            # CRUD自动生成的代码/数据库脚本（新增核心）
│           ├── frontend/              # 生成的PC端CRUD页面代码
│           ├── backend/               # 生成的后端CRUD接口代码
│           └── sql/                   # 生成的建表/索引/外键SQL脚本
├── .github/                           # GitHub标准目录（兼容GitHub Actions）
│   └── workflows/                     # 与.cicd/github-actions目录保持一致（软链/复制）
│       ├── ci.yml
│       ├── cd-test.yml
│       ├── cd-prod.yml
│       ├── cd-ai-studio.yml
│       └── crud-hot-update.yml        # CRUD热更新流水线（新增核心）
│
# -------------------------- 【Taro多端前端】：专注H5/小程序/APP --------------------------
├── frontend/                          # 仅保留Taro多端相关，移除PC端
│   ├── package.json
│   ├── turbo.json
│   ├── tsconfig.json                  # Taro全局TS配置（子项目继承）
│   ├── .eslintrc.js                   # Taro前端ESLint配置
│   ├── .prettierrc                    # Taro前端格式化配置
│   ├── apps/                          # Taro业务应用（多端）
│   │   ├── app-h5/                    # H5端：Taro编译目标，含page/组件/业务逻辑
│   │   ├── app-weapp/                 # 微信小程序端：Taro编译目标
│   │   └── app-alipay/                # 支付宝小程序端（扩展）
│   ├── packages/                      # Taro多端公共复用包（PC端/AI Studio也可复用）
│   │   ├── ui-components/             # 跨端UI组件库：Taro通用（小程序/H5）
│   │   ├── api-client/                # API请求封装：对接后端接口，TS类型约束（全局复用）
│   │   ├── utils/                     # 通用工具：日期、加密、格式转换（全局复用）
│   │   ├── types/                     # 全局TS类型：前后端通用DTO/枚举（全局复用）
│   │   └── config/                    # Taro配置：环境变量映射、接口前缀
│   ├── config/                        # 构建配置：Taro、Webpack、Vite
│   ├── scripts/                       # Taro专属脚本：多端编译、本地调试
│   └── tests/                         # Taro测试：单元测试（Jest）、E2E测试（Cypress）
│
# PC中后台系统（独立模块）
pc-admin/
├── README.md                          # PC端说明：design-token体系、bootstrap启动流程、30+子应用开发规范
├── package.json                       # 项目依赖：核心框架、Tailwind CSS、design-token工具、按需加载插件
├── tsconfig.json                      # 全局TS配置：多应用共享类型、design-token类型、路径别名
├── .eslintrc.js                       # ESLint配置：多应用统一代码规范、design-token使用校验
├── .prettierrc                        # 格式化配置：跨应用代码风格统一
├── .env                               # 环境变量：API前缀、应用列表、主题配置、懒加载策略（加入.gitignore）
├── .env.example                       # 环境变量示例：应用注册配置、design-token CDN地址
│
# -------------------------- 【1. 原子化设计体系：Design Token核心（Tailwind思路）】 --------------------------
├── design-token/                      # 全局Design Token体系（原子化设计核心，参考Tailwind）
│   ├── package.json                   # token工具依赖：style-dictionary、tailwindcss、postcss
│   ├── tsconfig.json                  # token类型配置：自动生成token类型声明
│   ├── config/                        # token配置：全局主题、多环境适配、子应用覆盖规则
│   │   ├── token.config.ts            # 核心配置：定义原子化token（颜色、间距、字体、圆角等）
│   │   ├── tailwind.config.ts         # Tailwind配置：基于token生成原子化类名（如bg-primary-500）
│   │   ├── theme.config.ts            # 主题配置：默认主题、暗黑主题、业务线专属主题（如运营/财务）
│   │   └── override.config.ts         # 子应用token覆盖规则：允许子应用自定义部分token，不破坏全局统一
│   ├── src/                           # token源码：按类型分类的原子化设计变量
│   │   ├── color/                     # 颜色token：品牌色、功能色、中性色（hex/rgb/hsl）
│   │   │   ├── brand.ts               # 品牌色：primary/secondary/tertiary
│   │   │   ├── functional.ts          # 功能色：success/warning/error/info
│   │   │   └── neutral.ts             # 中性色：black/white/gray系列
│   │   ├── spacing/                   # 间距token：0/2/4/8/12...（px/rem）
│   │   │   └── index.ts
│   │   ├── typography/                # 排版token：字体、字号、行高、字重
│   │   │   ├── font-family.ts
│   │   │   ├── font-size.ts
│   │   │   └── font-weight.ts
│   │   ├── border/                    # 边框token：圆角、边框宽度、边框样式
│   │   │   ├── radius.ts
│   │   │   └── width.ts
│   │   ├── shadow/                    # 阴影token：shadow-sm/shadow-md/shadow-lg
│   │   │   └── index.ts
│   │   └── z-index/                   # 层级token：z-10/z-20/.../z-500
│   │       └── index.ts
│   ├── generate/                      # token生成脚本：自动生成CSS/TS/JSON格式的token
│   │   ├── to-css.ts                  # 生成全局CSS变量（--color-primary-500）
│   │   ├── to-tailwind.ts             # 生成Tailwind自定义类名（映射token到Tailwind工具类）
│   │   ├── to-ts.ts                   # 生成TS类型声明（方便代码中引用token，如TokenTypes.Color.Primary500）
│   │   └── build.ts                   # 一键构建所有token格式
│   ├── dist/                          # token构建产物（自动生成，加入.gitignore）
│   │   ├── tokens.css                 # 全局CSS变量文件
│   │   ├── tokens.d.ts                # TS类型声明文件
│   │   ├── tokens.tailwind.js         # Tailwind配置扩展文件
│   │   └── tokens.json                # JSON格式token（供子应用动态加载）
│   └── tests/                         # token测试：校验token值、主题切换、子应用覆盖规则
│
# -------------------------- 【2. 统一启动入口：Bootstrap（适配30+子应用）】 --------------------------
├── bootstrap/                         # 所有应用的统一启动模块（优化大规模应用加载性能）
│   ├── package.json                   # bootstrap专属依赖：应用懒加载、缓存、token注入
│   ├── tsconfig.json                  # bootstrap TS配置
│   ├── src/
│   │   ├── index.tsx                  # 全局启动入口：加载token→配置懒加载→注册应用→启动核心
│   │   ├── App.tsx                    # 根组件：挂载应用容器、路由容器、全局token注入
│   │   ├── config/                    # 启动配置：适配30+应用的加载策略
│   │   │   ├── appRegistry.ts         # 应用注册表：按业务线分组（如运营/财务/供应链），声明应用核心信息
│   │   │   ├── globalConfig.ts        # 全局配置：token主题、30+应用加载策略（懒加载/预加载/缓存）
│   │   │   ├── loadEnv.ts             # 环境变量加载：区分开发/测试/生产（生产环境开启应用缓存）
│   │   │   └── performance.ts         # 性能配置：应用加载超时、并发加载数（默认同时加载≤5个）
│   │   ├── launcher/                  # 应用启动器：优化30+应用加载性能
│   │   │   ├── AppLauncher.tsx        # 应用启动器：按业务线懒加载、缓存已加载应用、预加载常用应用
│   │   │   ├── RouteLauncher.tsx      # 路由启动器：动态注册路由、路由懒加载、路由缓存
│   │   │   ├── Lifecycle.tsx          # 生命周期管理：应用加载/卸载/激活/失活（卸载不常用应用释放内存）
│   │   │   └── CacheManager.tsx       # 应用缓存管理器：缓存已加载应用的组件/状态，避免重复加载
│   │   ├── plugin/                    # 全局插件：注入token、优化30+应用性能
│   │   │   ├── authPlugin.ts          # 权限插件：按角色过滤应用列表（减少加载量）
│   │   │   ├── requestPlugin.ts       # 请求插件：统一API请求、拦截器、请求缓存
│   │   │   ├── tokenPlugin.ts         # Token插件：全局注入design-token、主题切换、子应用token隔离
│   │   │   ├── errorPlugin.ts         # 错误插件：全局错误捕获、30+应用异常隔离
│   │   │   └── performancePlugin.ts   # 性能插件：监控应用加载时间、内存占用、上报性能数据
│   │   └── types/                     # bootstrap类型：应用声明、token类型、生命周期类型
│   └── tests/                         # bootstrap测试：启动流程、30+应用加载性能、token注入
│
# -------------------------- 【3. 核心框架层：基于Token的通用能力（30+应用复用）】 --------------------------
├── core/                              # 所有应用共享的核心能力（基于design-token封装，无业务逻辑）
│   ├── package.json                   # core专属依赖：基于token的通用组件、hooks
│   ├── tsconfig.json                  # core TS配置
│   ├── src/
│   │   ├── components/                # 全局通用组件：基于design-token+Tailwind封装，样式统一
│   │   │   ├── layout/                # 全局布局：侧边栏、头部、标签页（适配30+应用切换）
│   │   │   │   ├── Sidebar.tsx        # 侧边栏：按业务线分组展示30+应用，支持搜索/收藏
│   │   │   │   ├── Header.tsx         # 头部：主题切换、用户信息、全局搜索（跨应用）
│   │   │   │   └── Tabs.tsx           # 标签页：缓存30+应用的打开状态，支持关闭/刷新
│   │   │   ├── form/                  # 通用表单：基于token的表单组件（ProForm+Tailwind）
│   │   │   ├── table/                 # 通用表格：基于token的表格组件（ProTable+Tailwind）
│   │   │   ├── modal/                 # 通用弹窗：基于token的弹窗组件（样式统一）
│   │   │   ├── button/                # 通用按钮：基于token的按钮（primary/secondary等，样式统一）
│   │   │   └── common/                # 基础组件：空状态、加载、异常页（基于token样式）
│   │   ├── hooks/                     # 全局通用Hooks：基于token、适配30+应用
│   │   │   ├── useToken.ts            # 获取/修改design-token，支持子应用局部覆盖
│   │   │   ├── useRequest.ts          # 统一请求Hook：缓存、防抖、适配30+应用接口隔离
│   │   │   ├── useAuth.ts             # 权限Hook：按角色过滤应用/按钮
│   │   │   ├── useTheme.ts            # 主题Hook：切换主题、同步token
│   │   │   └── useApp.ts              # 应用Hook：获取应用信息、触发应用生命周期
│   │   ├── utils/                     # 全局工具：基于token、适配30+应用
│   │   │   ├── token.ts               # token工具：获取/转换token值、验证token规则
│   │   │   ├── format.ts              # 格式化工具：日期、数字（基于token的排版规则）
│   │   │   ├── cache.ts               # 缓存工具：30+应用的状态缓存、本地存储
│   │   │   └── performance.ts         # 性能工具：监控组件渲染、优化30+应用性能
│   │   ├── api/                       # 全局API：登录、用户、权限、应用管理（通用接口）
│   │   ├── models/                    # 全局状态：用户信息、权限、主题、应用列表（适配30+应用）
│   │   ├── styles/                    # 全局样式：基于token的基础样式、Tailwind入口
│   │   │   ├── index.css              # 全局样式入口：引入token CSS变量、Tailwind基础样式
│   │   │   └── reset.css              # 重置样式：统一30+应用的基础样式
│   │   └── types/                     # 全局类型：通用DTO、token类型、组件Props类型
│   └── tests/                         # core测试：通用组件、hooks、token适配
│
# -------------------------- 【4. 业务应用层：30+子应用（隔离+Token复用）】 --------------------------
├── apps/                              # 业务应用目录：30+子应用，独立开发，复用core+token
│   ├── _templates/                    # 应用模板：新建子应用时快速初始化（基于token+core）
│   │   └── app-template/              # 模板结构：包含基础的token覆盖、路由、页面
│   ├── app-system/                    # 系统管理应用：用户、角色、菜单（示例应用1）
│   │   ├── package.json               # 应用专属依赖：仅业务特有依赖（复用core/token）
│   │   ├── tsconfig.json              # 应用TS配置（继承全局，声明token类型）
│   │   ├── design-token/              # 应用专属token：局部覆盖全局token（不影响其他应用）
│   │   │   ├── override.ts            # 覆盖规则：仅修改本应用的部分token（如品牌色）
│   │   │   └── local-tokens.css       # 本地token CSS：仅作用于本应用
│   │   ├── src/
│   │   │   ├── index.tsx              # 应用入口：导出应用声明、路由、生命周期钩子（懒加载）
│   │   │   ├── app.ts                 # 应用声明：名称、图标、权限、业务线、token覆盖规则
│   │   │   ├── routes/                # 应用路由：懒加载路由，减少初始加载体积
│   │   │   ├── pages/                 # 应用页面：使用core组件+token/Tailwind，样式统一
│   │   │   ├── components/            # 应用专属组件：基于token封装，仅本应用使用
│   │   │   ├── api/                   # 应用专属API：仅本应用的业务接口（隔离）
│   │   │   ├── models/                # 应用专属状态：仅本应用的业务状态（隔离）
│   │   │   ├── utils/                 # 应用专属工具：仅本应用的业务工具（隔离）
│   │   │   ├── types/                 # 应用专属类型：仅本应用的业务类型（隔离）
│   │   │   └── styles/                # 应用专属样式：基于token/Tailwind，仅作用于本应用
│   │   ├── tests/                     # 应用测试：页面、组件、API测试（隔离）
│   │   └── README.md                  # 应用说明：功能、token覆盖规则、开发规范
│   ├── app-operation/                 # 运营管理应用（示例应用2）
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   ├── design-token/              # 运营业务线专属token覆盖
│   │   ├── src/                       # 结构同app-system，隔离业务逻辑
│   │   ├── tests/
│   │   └── README.md
│   ├── app-finance/                   # 财务管理应用（示例应用3）
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   ├── design-token/              # 财务业务线专属token覆盖
│   │   ├── src/                       # 结构同app-system，隔离业务逻辑
│   │   ├── tests/
│   │   └── README.md
│   ├── ...                            # 其余27+个子应用（按业务线分组，如供应链/仓储/客服等）
│   └── app-generated/                 # AI生成的CRUD应用：自动生成，基于token+core
│       ├── package.json
│       ├── tsconfig.json
│       ├── design-token/              # 生成应用的token覆盖（可选）
│       ├── src/
│       │   ├── index.tsx              # 生成的应用入口：自动注册到bootstrap（懒加载）
│       │   ├── app.ts                 # 生成的应用声明：自动填充名称/权限/token规则
│       │   ├── routes/                # 生成的路由：按表名自动生成（懒加载）
│       │   ├── pages/                 # 生成的CRUD页面：基于core组件+token/Tailwind
│       │   ├── api/                   # 生成的API：对接后端CRUD接口
│       │   └── types/                 # 生成的类型：自动映射数据库字段+token类型
│       └── README.md                  # 生成说明：表名、生成时间、token使用规则
│
# -------------------------- 【5. AI生成代码目录：热更新目标（适配30+应用）】 --------------------------
├── generated/                         # AI-Studio生成的CRUD代码目录（热更新核心）
│   ├── crud/                          # 按表名分类存储生成的代码（基于token+Tailwind）
│   │   ├── table-order/               # 订单表CRUD代码：页面、组件、API（使用core组件+token）
│   │   ├── table-product/             # 商品表CRUD代码：页面、组件、API
│   │   └── ...                        # 其余生成的表CRUD代码
│   ├── registry.ts                    # 生成代码注册表：自动注册到app-generated应用（懒加载）
│   └── token-override.ts              # 生成代码的token覆盖规则：统一样式，支持局部调整
│
# -------------------------- 【6. 构建配置层：适配30+应用+Token构建】 --------------------------
├── config/                            # 构建配置：Vite/Webpack、30+应用按需构建、token构建
│   ├── vite.config.ts                 # Vite主配置：多应用构建、token注入、性能优化
│   ├── vite.app-config.ts             # 应用构建配置：按业务线构建（如运营组/财务组）、单个应用构建
│   ├── vite.token-config.ts           # Token构建配置：自动生成token CSS/TS，注入Tailwind
│   ├── proxy.ts                       # 代理配置：30+应用API代理、环境区分
│   ├── define.ts                      # 全局常量：应用列表、token主题、性能配置
│   ├── splitChunks.ts                 # 代码分割配置：拆分core/token/bootstrap，30+应用单独打包
│   └── hot-update.ts                  # 热更新配置：监听generated目录、30+应用热更新隔离
│
# -------------------------- 【7. 脚本层：30+应用开发/构建/部署（适配Token）】 --------------------------
├── scripts/                           # 专属脚本：适配30+应用、token构建、热更新
│   ├── dev.js                         # 开发启动脚本：启动bootstrap，懒加载30+应用，开启token热更新
│   ├── build.js                       # 构建脚本：可构建单个应用/业务线应用（如npm run build app-system）
│   ├── build-all.js                   # 全量构建脚本：构建30+应用，拆分chunk，优化加载
│   ├── build-token.js                 # Token构建脚本：一键生成所有格式的token（CSS/TS/JSON）
│   ├── start-bootstrap.js             # 仅启动bootstrap：调试启动流程、token注入
│   ├── crud-hot-update.js             # CRUD热更新脚本：替换generated目录，重启对应应用（不影响其他应用）
│   ├── performance-analyze.js         # 性能分析脚本：分析30+应用加载性能、打包体积
│   └── test.js                        # 测试脚本：可测试单个应用/业务线应用/全部应用
│
# -------------------------- 【8. 静态资源层：30+应用共享（Token适配）】 --------------------------
├── public/                            # 全局静态资源：图标、字体、第三方库（基于token适配）
│   ├── favicon.ico
│   ├── icons/                         # 应用图标：每个应用的专属图标（基于token的颜色适配）
│   ├── fonts/                         # 全局字体：匹配typography token的字体文件
│   ├── themes/                        # 主题资源：暗黑/浅色主题的静态资源（如背景图）
│   └── libs/                          # 第三方库：如echarts（基于token适配样式）
│
# -------------------------- 【9. 测试层：30+应用统一测试（Token覆盖）】 --------------------------
├── tests/                             # 全局测试：E2E测试、集成测试、token测试
│   ├── e2e/                           # E2E测试：覆盖30+应用核心流程、token切换、性能
│   │   ├── bootstrap.test.ts          # bootstrap启动测试：30+应用加载性能
│   │   ├── token.test.ts              # token测试：主题切换、子应用覆盖、样式统一
│   │   ├── app-system.test.ts         # 系统应用核心流程测试
│   │   └── crud-generated.test.ts     # 生成的CRUD应用测试
│   ├── mocks/                         # 测试Mock：API Mock、应用Mock、token Mock
│   └── utils/                         # 测试工具：通用测试方法、token校验、性能监控
│
# -------------------------- 【10. 部署配置层：30+应用统一部署（Token适配）】 --------------------------
├── deploy/                            # 部署配置：Docker、Nginx、热更新（适配30+应用）
│   ├── Dockerfile                     # 多应用Dockerfile：支持bootstrap启动、token主题切换、30+应用隔离
│   ├── docker-compose.yml             # 本地部署：bootstrap+30+应用+依赖服务（按需启动）
│   ├── nginx.conf                     # Nginx配置：30+应用路由转发、静态资源缓存、token主题配置
│   ├── hot-update.sh                  # 生产热更新脚本：替换generated目录，仅重启对应应用（不影响其他应用）
│   └── performance.yml                # 部署性能配置：Nginx缓存、应用加载策略、token CDN配置
└── dist/                              # 构建产物目录（加入.gitignore）
│  ├── bootstrap/                     # bootstrap构建产物（缓存，不常更新）
│  ├── core/                          # core构建产物（缓存，基于token，不常更新）
│  ├── design-token/                  # token构建产物（缓存，主题切换时更新）
│  ├── apps/                          # 各应用构建产物：按应用名/业务线分类（30+应用）
│  │   ├── app-system/
│  │   ├── app-operation/
│  │   ├── app-generated/
│  │   └── ...
│  └── assets/                        # 共享静态资源产物（基于token适配）
│  
# -------------------------- 【AI Studio基座】：CRUD自动生成全流程核心（重构成业务专属） --------------------------
├── ai-studio/                         # AI Studio基座：CRUD自动生成+审核+部署+热更新
│   ├── README.md                      # AI Studio说明：CRUD生成流程、审核规范、热更新规则
│   ├── frontend/                      # AI Studio前端：CRUD需求提交/审核/预览/发布全流程界面
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   ├── src/
│   │   │   ├── api/                   # AI Studio API请求：对接基座后端
│   │   │   ├── assets/                # 静态资源：表单模板、图标、预览样式
│   │   │   ├── components/            # 核心组件（CRUD生成专属）
│   │   │   │   ├── crud-creator/      # CRUD需求创建核心组件（新增核心）
│   │   │   │   │   ├── TableNameInput.tsx # 表名输入/文件导入组件
│   │   │   │   │   ├── FieldParser.tsx    # 字段解析+格式化组件
│   │   │   │   │   ├── FieldConfirm.tsx   # 字段确认+补充细节组件
│   │   │   │   │   └── ImportFile.tsx     # 表结构文件导入组件（Excel/JSON/SQL）
│   │   │   │   ├── crud-audit/        # IT审核组件（新增核心）
│   │   │   │   │   ├── AuditList.tsx      # 审核列表：待审核/已通过/已驳回CRUD需求
│   │   │   │   │   ├── AuditDetail.tsx    # 审核详情：微调字段/表结构/代码生成规则
│   │   │   │   │   └── AuditAction.tsx    # 审核操作：通过/驳回/微调提交
│   │   │   │   ├── crud-preview/      # CRUD生成预览组件（新增核心）
│   │   │   │   │   ├── CodePreview.tsx    # 生成代码预览：前端/后端/SQL
│   │   │   │   │   ├── TestEnvPreview.tsx # 测试环境预览：跳转测试环境查看生成效果
│   │   │   │   │   └── DiffViewer.tsx     # 代码差异对比：生成代码vs项目规范
│   │   │   │   ├── crud-release/      # CRUD发布组件（新增核心）
│   │   │   │   │   ├── ReleaseList.tsx    # 发布列表：待发布/已发布/已回滚CRUD需求
│   │   │   │   │   ├── ReleaseConfirm.tsx # 生产发布确认：IT最终确认、热更新授权
│   │   │   │   │   └── ReleaseStatus.tsx  # 发布状态：测试中/已发布/热更新中
│   │   │   │   └── common/            # 通用组件：按钮、表单、弹窗、加载
│   │   │   ├── pages/                 # 业务页面（CRUD生成专属）
│   │   │   │   ├── home/              # 首页：CRUD创建入口、待办审核/发布提醒
│   │   │   │   ├── crud-create/       # CRUD需求创建页：填表名→解析字段→确认细节
│   │   │   │   ├── crud-audit/        # CRUD审核页：IT审核、微调、授权生成
│   │   │   │   ├── crud-preview/      # CRUD预览页：测试环境查看生成效果、确认发布
│   │   │   │   └── crud-manage/       # CRUD管理页：所有生成记录、版本回滚、日志
│   │   │   ├── models/                # 状态管理：CRUD需求、审核状态、发布状态
│   │   │   ├── utils/                 # 前端工具：字段解析、格式校验、代码高亮
│   │   │   ├── types/                 # 前端类型：CRUD需求、字段信息、审核记录、发布状态
│   │   │   │   ├── crud.d.ts          # CRUD核心类型：表信息、字段信息、生成规则
│   │   │   │   ├── audit.d.ts         # 审核类型：审核记录、操作日志、状态枚举
│   │   │   │   └── release.d.ts       # 发布类型：发布记录、热更新状态、环境信息
│   │   │   └── app.tsx                # 入口文件
│   │   ├── config/                    # 构建配置：Vite/Webpack、路由、环境变量
│   │   └── tests/                     # 前端测试：单元测试、E2E测试
│   ├── backend/                       # AI Studio后端：CRUD生成+审核+部署+热更新核心逻辑
│   │   ├── pom.xml                    # 父POM：依赖版本管理、模块聚合
│   │   ├── common/                    # 公共模块
│   │   │   ├── core/                  # 核心工具：常量、异常、注解、通用DTO
│   │   │   ├── security/              # 安全：鉴权（用户/IT角色）、Token、操作日志
│   │   │   ├── field-parser/          # 字段解析核心：识别导入/输入的字段、格式化（新增核心）
│   │   │   │   ├── ExcelParser.java   # Excel表结构解析
│   │   │   │   ├── SQLParser.java     # SQL建表语句解析
│   │   │   │   ├── JSONParser.java    # JSON字段结构解析
│   │   │   │   └── FieldFormatter.java # 字段格式化：类型映射、命名规范
│   │   │   └── client/                # 客户端：业务后端/数据库/CI/CD客户端
│   │   ├── api/                       # 接口层：Controller、请求校验、OpenAPI文档
│   │   │   ├── crud-demand/           # CRUD需求管理接口（新增核心）
│   │   │   │   ├── DemandController.java # 需求创建、查询、更新、删除
│   │   │   │   └── FieldController.java   # 字段解析、确认、微调接口
│   │   │   ├── crud-audit/            # CRUD审核接口（新增核心）
│   │   │   │   └── AuditController.java   # 审核列表、详情、通过/驳回/微调接口
│   │   │   ├── crud-generate/         # CRUD代码生成接口（新增核心）
│   │   │   │   └── GenerateController.java # 代码生成、数据库脚本生成、测试环境部署
│   │   │   └── crud-release/          # CRUD发布接口（新增核心）
│   │   │       └── ReleaseController.java # 测试环境确认、生产热更新、发布记录查询
│   │   ├── service/                   # 业务层：核心逻辑实现
│   │   │   ├── crud-demand/           # CRUD需求管理逻辑
│   │   │   │   ├── DemandService.java     # 需求CRUD、状态流转
│   │   │   │   └── DemandLogService.java  # 需求操作日志
│   │   │   ├── crud-audit/            # CRUD审核逻辑
│   │   │   │   ├── AuditService.java      # 审核规则、状态更新、IT权限校验
│   │   │   │   └── AuditLogService.java   # 审核操作日志
│   │   │   ├── crud-generate/         # CRUD代码生成核心逻辑（新增核心）
│   │   │   │   ├── CodeGenerateService.java # 前后端代码生成：按项目规范生成CRUD
│   │   │   │   ├── SQLGenerateService.java  # 数据库脚本生成：建表、索引、外键
│   │   │   │   ├── TemplateService.java    # 模板管理：加载/渲染代码生成模板
│   │   │   │   └── TestEnvDeployService.java # 测试环境部署：推送代码、执行SQL、启动服务
│   │   │   └── crud-release/          # CRUD发布逻辑（新增核心）
│   │   │       ├── ReleaseService.java     # 生产热更新：代码替换、SQL执行、服务重启
│   │   │       ├── HotUpdateService.java    # 热更新核心：无停机替换代码、数据库变更
│   │   │       └── ReleaseLogService.java   # 发布/热更新日志
│   │   ├── model/                     # 数据层：Entity、Mapper、SQL脚本
│   │   │   ├── crud-demand/           # CRUD需求数据模型
│   │   │   │   ├── CrudDemand.java        # 需求主表：表名、创建人、状态、字段信息
│   │   │   │   ├── CrudField.java         # 字段信息表：字段名、类型、长度、备注、外键
│   │   │   │   └── CrudDemandLog.java     # 需求操作日志表
│   │   │   ├── crud-audit/            # 审核数据模型
│   │   │   │   ├── CrudAudit.java         # 审核记录表：需求ID、审核人、审核状态、微调内容
│   │   │   │   └── CrudAuditLog.java      # 审核操作日志表
│   │   │   ├── crud-generate/         # 生成记录数据模型
│   │   │   │   ├── CrudGenerateRecord.java # 生成记录表：需求ID、代码路径、SQL路径、版本
│   │   │   │   └── CrudTestEnvRecord.java  # 测试环境部署记录表
│   │   │   └── crud-release/          # 发布记录数据模型
│   │   │       ├── CrudReleaseRecord.java  # 发布记录表：需求ID、发布人、发布时间、环境
│   │   │       └── CrudHotUpdateLog.java   # 热更新日志表：更新内容、状态、耗时
│   │   ├── generator/                 # CRUD代码生成器核心（新增核心）
│   │   │   ├── template/              # 代码生成模板：按项目规范定制
│   │   │   │   ├── frontend/          # 前端模板：PC端CRUD页面、API、状态管理
│   │   │   │   │   ├── crud-page.ftl      # CRUD页面模板（Freemarker）
│   │   │   │   │   ├── api.ftl            # API请求模板
│   │   │   │   │   └── model.ftl          # 状态管理模板
│   │   │   │   ├── backend/           # 后端模板：Controller、Service、Mapper、Entity
│   │   │   │   │   ├── controller.ftl     # 控制器模板
│   │   │   │   │   ├── service.ftl        # 服务层模板
│   │   │   │   │   ├── mapper.ftl         # Mapper模板
│   │   │   │   │   ├── entity.ftl         # 实体类模板
│   │   │   │   │   └── dto.ftl            # DTO模板
│   │   │   │   └── sql/               # SQL模板：建表、索引、外键、初始化
│   │   │   │       ├── create-table.ftl   # 建表语句模板
│   │   │   │       ├── index.ftl          # 索引创建模板
│   │   │   │       └── foreign-key.ftl    # 外键创建模板
│   │   │   ├── rule/                  # 生成规则：命名规范、代码风格、数据库规范
│   │   │   │   ├── NamingRule.java        # 命名规则：表名→类名、字段名→属性名
│   │   │   │   ├── CodeStyleRule.java     # 代码风格规则：缩进、注释、导入包
│   │   │   │   └── DatabaseRule.java      # 数据库规则：字段类型映射、索引规范
│   │   │   └── executor/              # 生成执行器：渲染模板、输出代码/SQL
│   │   │       ├── FrontendGenerator.java # 前端代码生成执行器
│   │   │       ├── BackendGenerator.java  # 后端代码生成执行器
│   │   │       └── SQLGenerator.java      # SQL脚本生成执行器
│   │   ├── deploy/                    # 部署核心：测试环境/生产环境部署
│   │   │   ├── test-env/              # 测试环境部署
│   │   │   │   ├── CodeDeployer.java      # 代码推送：复制生成代码到测试环境目录
│   │   │   │   ├── SQLExecutor.java       # SQL执行：在测试库执行建表/索引/外键脚本
│   │   │   │   └── ServiceRestarter.java  # 服务重启：重启测试环境前后端服务
│   │   │   └── prod-env/              # 生产环境热更新
│   │   │       ├── HotUpdateDeployer.java # 热更新部署：无停机替换代码
│   │   │       ├── ProdSQLExecutor.java   # 生产SQL执行：事务包裹、增量执行
│   │   │       └── RollbackService.java   # 回滚服务：热更新失败时回滚代码/SQL
│   │   ├── bootstrap/                 # 启动模块：SpringBoot主类、配置扫描
│   │   ├── config/                    # 配置类：数据源、代码生成规则、部署路径、热更新配置
│   │   │   ├── GeneratorConfig.java       # 代码生成配置：模板路径、输出路径、命名规则
│   │   │   ├── DeployConfig.java          # 部署配置：测试/生产环境路径、数据库信息
│   │   │   └── HotUpdateConfig.java       # 热更新配置：更新策略、备份路径、超时时间
│   │   └── tests/                     # 后端测试：单元测试、集成测试（覆盖CRUD生成全流程）
│   ├── template/                      # 代码生成模板库（可扩展）
│   │   ├── frontend-pc/               # PC端模板：适配pc-admin的Ant Design Pro
│   │   ├── backend-springboot/        # 后端模板：适配backend的SpringBoot多模块
│   │   └── sql-mysql/                 # SQL模板：适配MySQL的建表/索引/外键
│   ├── config/                        # AI Studio全局配置：生成规则、部署路径、审核流程
│   │   ├── generate-rule.yml          # 代码生成规则：命名规范、模板选择
│   │   ├── deploy-path.yml            # 部署路径：测试/生产环境代码/数据库路径
│   │   └── audit-rule.yml             # 审核规则：必填字段、字段类型校验、IT角色权限
│   └── scripts/                       # AI Studio专属脚本
│       ├── init-template.sh           # 初始化模板库：复制默认模板到生成目录
│       ├── test-env-preview.sh        # 测试环境预览：打开生成的CRUD页面链接
│       └── prod-hot-update-check.sh   # 生产热更新前置检查：代码规范、数据库权限、备份
│
# -------------------------- 【后端开发目录】：SpringBoot + Nacos + Redis + Keycloak --------------------------
├── backend/                           # 原有业务后端（CRUD生成的后端代码将集成到此处）
│   ├── pom.xml                        # 父POM：统一依赖版本、模块聚合
│   ├── README.md                      # 后端说明：启动方式、模块依赖、数据库脚本
│   ├── common/                        # 后端公共模块
│   │   ├── common-core/               # 核心工具：常量、异常、注解、通用DTO
│   │   ├── common-redis/              # Redis封装：序列化、分布式锁、缓存注解
│   │   ├── common-nacos/              # Nacos集成：配置中心、服务发现
│   │   ├── common-keycloak/           # Keycloak集成：权限校验、用户解析
│   │   └── common-mybatis/            # MyBatis封装：分页、通用Mapper
│   ├── api/                           # 接口层：Controller、请求校验、API文档（Swagger）
│   │   └── generated/                 # AI生成的CRUD接口代码（热更新目标目录，新增核心）
│   │       └── crud/                  # 按表名分类存储生成的Controller/Service
│   ├── service/                       # 业务层：Service实现、业务逻辑
│   │   └── generated/                 # AI生成的CRUD服务代码（新增核心）
│   │       └── crud/                  # 按表名分类存储生成的Service
│   ├── model/                         # 数据层：Entity、POJO、Mapper、SQL脚本
│   │   └── generated/                 # AI生成的实体类/Mapper（新增核心）
│   │       └── crud/                  # 按表名分类存储生成的Entity/Mapper
│   ├── sql/                           # 数据库脚本
│   │   └── generated/                 # AI生成的建表/索引/外键SQL（新增核心）
│   │       └── crud/                  # 按表名分类存储生成的SQL
│   ├── config/                        # SpringBoot配置类：数据源、Redis、Keycloak
│   ├── bootstrap/                     # 启动模块：SpringBoot主类、配置扫描
│   ├── scripts/                       # 后端专属脚本：数据库初始化、Nacos配置导入、CRUD热更新
│   │   └── crud-hot-update.sh         # 后端CRUD代码热更新：替换生成代码、重启后端服务
│   ├── tests/                         # 后端测试：单元测试（JUnit）、集成测试
│   └── hot-update/                    # 热更新备份目录：更新前的代码/SQL备份（新增核心）
│
# -------------------------- 【辅助项目目录】：Python + Go --------------------------
├── auxiliary/                         # 原有辅助项目（可扩展CRUD生成辅助工具）
│   ├── python/                        # Python辅助项目
│   │   ├── crawler/                   # 数据爬取/同步
│   │   ├── data-process/              # 批量数据处理/统计
│   │   ├── test-automation/           # 自动化测试（接口/UI）
│   │   ├── crud-validator/            # CRUD生成代码校验工具（新增核心）
│   │   │   ├── code_validate.py       # 校验生成代码是否符合项目规范
│   │   │   └── sql_validate.py        # 校验生成SQL是否安全、符合索引规范
│   │   ├── requirements.txt           # Python依赖
│   │   ├── setup.py                   # Python打包配置
│   │   ├── tests/                     # Python单元测试（pytest）
│   │   └── scripts/                   # Python运行脚本
│   └── go/                            # Go辅助项目
│       ├── tool-cli/                  # 命令行工具：配置生成、日志分析
│       ├── crud-hotupdate-cli/        # CRUD热更新CLI工具：手动触发热更新、回滚（新增核心）
│       ├── service-proxy/             # 轻量代理服务：对接第三方接口
│       ├── go.mod                     # Go模块配置
│       ├── go.sum
│       ├── tests/                     # Go单元测试
│       └── scripts/                   # Go编译/运行脚本
│
# -------------------------- 【基础设施目录】：Docker + 中间件配置 --------------------------
├── infra/                             # 全局基础设施
│   ├── docker/                        # Docker配置：适配开发/CI/CD/生产
│   │   ├── frontend/                  # Taro多端Dockerfile：多阶段构建（构建+运行）
│   │   ├── pc-admin/                  # PC中后台Dockerfile
│   │   │   └── Dockerfile             # 新增热更新支持：挂载generated目录
│   │   ├── backend/                   # 业务后端Dockerfile：分层构建（依赖+运行）
│   │   │   └── Dockerfile             # 新增热更新支持：挂载generated目录
│   │   ├── ai-studio/                 # AI Studio Dockerfile（前端+后端）
│   │   ├── auxiliary/                 # Python/Go辅助项目Dockerfile
│   │   ├── middleware/                # 中间件Docker配置：nacos/Redis/Keycloak
│   │   └── compose/                   # 环境专属compose：开发/测试/生产
│   │       ├── dev.yml
│   │       ├── test.yml
│   │       ├── prod.yml
│   │       └── ai-studio-dev.yml      # AI Studio本地开发中间件配置：数据库/代码生成依赖
│   ├── nacos/                         # Nacos配置：命名空间、配置集、服务列表
│   │   └── crud-config/               # CRUD生成配置：代码生成规则、部署路径（新增）
│   ├── redis/                         # Redis配置：持久化、集群、过期策略
│   ├── keycloak/                      # Keycloak配置：Realm、客户端、权限规则
│   │   └── crud-roles/                # CRUD角色配置：普通用户（提交）、IT（审核/发布）（新增）
│   └── k8s/                           # Kubernetes配置（若用K8s部署）
│       ├── frontend/                  # Taro多端Deployment/Service/Ingress
│       ├── pc-admin/                  # PC中后台Deployment/Service/Ingress
│       │   └── crud-hotupdate.yaml    # PC端CRUD热更新配置：挂载generated目录（新增）
│       ├── backend/                   # 业务后端Deployment/Service/ConfigMap
│       │   └── crud-hotupdate.yaml    # 后端CRUD热更新配置：挂载generated目录、SQL执行权限（新增）
│       ├── ai-studio/                 # AI Studio基座K8s配置
│       └── middleware/                # 中间件StatefulSet/Service
│
# -------------------------- 【文档目录】：开发→发布全流程文档 --------------------------
├── docs/
│   ├── development/                   # 开发文档
│   │   ├── coding-standard.md         # 编码规范：前端/后端/Python/Go
│   │   ├── environment-setup.md       # 环境搭建：本地开发环境配置步骤
│   │   ├── debug-guide.md             # 调试指南：各技术栈调试方法
│   │   └── ai-studio-dev.md           # AI Studio开发指南：CRUD生成流程、扩展规范
│   ├── architecture/                  # 架构文档
│   │   ├── overall-arch.md            # 整体架构：模块依赖、技术选型、数据流
│   │   ├── frontend-arch.md           # Taro多端架构：Turbo monorepo、多端适配
│   │   ├── pc-admin-arch.md           # PC中后台架构：Ant Design Pro、权限设计
│   │   ├── backend-arch.md            # 业务后端架构：SpringBoot多模块、中间件集成
│   │   ├── ai-studio-arch.md          # AI Studio架构：CRUD生成流程、代码生成器、热更新（新增核心）
│   │   │   ├── crud-flow.mmd          # CRUD生成流程图（Mermaid）：提交→审核→生成→测试→发布
│   │   │   ├── code-generator.md      # 代码生成器设计：模板引擎、规则引擎
│   │   │   └── hot-update.md          # 热更新设计：无停机更新、回滚机制
│   │   └── auxiliary-arch.md          # 辅助项目架构
│   ├── api/                           # API文档
│   │   ├── openapi.json               # 业务后端Swagger/OpenAPI导出文件
│   │   ├── ai-studio-openapi.json     # AI Studio OpenAPI导出文件
│   │   ├── crud-api.md                # CRUD生成相关API文档：提交/审核/生成/发布（新增核心）
│   │   ├── api-test-guide.md          # API测试指南
│   │   └── version-history.md         # API版本历史
│   ├── cicd/                          # CI/CD文档
│   │   ├── github-actions-guide.md    # GitHub Actions使用指南
│   │   ├── jenkins-guide.md           # Jenkins配置/使用指南
│   │   ├── release-process.md         # 发布流程：从提测到生产的全步骤
│   │   ├── ai-studio-cicd-guide.md    # AI Studio CI/CD指南：构建/部署/扩展
│   │   └── crud-hot-update-guide.md   # CRUD热更新指南：操作步骤、回滚方案、注意事项（新增核心）
│   ├── deploy/                        # 部署文档
│   │   ├── test-env.md                # 测试环境：服务器配置、部署路径、访问方式
│   │   ├── prod-env.md                # 生产环境：服务器配置、安全策略、监控告警
│   │   ├── ai-studio-test-env.md      # AI Studio测试环境：资源配置、运行时隔离
│   │   ├── ai-studio-prod-env.md      # AI Studio生产环境：集群、资源调度、容灾
│   │   ├── crud-deploy.md             # CRUD部署指南：测试环境部署、生产热更新（新增核心）
│   │   └── rollback-guide.md          # 回滚指南：手动/自动回滚操作步骤
│   └── middleware/                    # 中间件文档
│       ├── nacos-guide.md             # Nacos使用/配置指南
│       ├── redis-guide.md             # Redis使用/缓存策略
│       ├── keycloak-guide.md          # Keycloak权限/配置指南
│       └── ai-studio-runtime-guide.md # AI Studio运行时：代码生成、热更新配置
│
# -------------------------- 【临时/产物目录】：加入.gitignore --------------------------
├── artifacts/                         # 本地构建产物（开发阶段）
│   ├── frontend/                      # Taro多端产物
│   ├── pc-admin/                      # PC中后台产物
│   ├── backend/                       # 业务后端产物
│   ├── ai-studio/                     # AI Studio产物
│   ├── auxiliary/                     # 辅助项目产物
│   └── crud-generated/                # CRUD自动生成的代码/数据库脚本
│       ├── frontend/                  # 生成的PC端CRUD页面代码
│       ├── backend/                   # 生成的后端CRUD接口代码
│       └── sql/                       # 生成的建表/索引/外键SQL脚本
├── logs/                              # 全局日志
│   ├── frontend/
│   ├── pc-admin/                      # PC中后台日志
│   ├── backend/
│   │   └── crud-hotupdate.log         # 后端CRUD热更新日志（新增）
│   ├── ai-studio/                     # AI Studio日志：前端/后端/代码生成/热更新
│   │   ├── crud-generate.log          # CRUD代码生成日志
│   │   ├── crud-audit.log             # CRUD审核日志
│   │   └── crud-hotupdate.log         # CRUD热更新日志
│   ├── auxiliary/
│   └── cicd/
│       └── crud-hotupdate.log         # CI/CD CRUD热更新日志（新增）
├── temp/                              # 临时文件：打包缓存、测试报告等
│   ├── ai-studio/                     # AI Studio临时文件：上传的表结构文件、生成的临时代码
│   ├── pc-admin/                      # PC中后台临时文件
│   └── crud-backup/                   # CRUD热更新备份：更新前的代码/SQL（新增核心）
└── backup/                            # 热更新备份目录（生产环境同步，新增核心）
    └── crud/                          # 按版本/时间戳存储CRUD热更新前的代码/SQL