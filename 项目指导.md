目前的设计只是这个庞大的全链路闭环系统的一个个小的环节，我希望你能够编写整理更强大的完整的文档指导系统（目前实际上只涵盖了其中一个闭环）

核心：作为个人系统，它的功能是一个完整的闭环链路，打通多端，兼顾开发环境和生产环境的大型项目

先盘点目前已有资源：

1：本地（windows开发环境）已经封装好的go-项目，位于目录C:\Program Files\SDKAgent下，提供多种SDK方便调用（包括本地ollama模型，qwen模型，skills，RAG256维度向量库），不过可用性待验证

2：数据库支持MongoDB，MySQL，postgreSQL，Redis

3：工作目录（E:\enterprise-workspace）包含各种待开发或者待完善的项目（前端或者后端）

4：已经安装到本地的cli（cursor，kimi，codex，claude）

5：远程Linux服务器47.112.163.152（已配对过，可以ssh连接，2核心4G内存）

6：本地已经部署好了Moltbot，并且飞书长连接插件可用，在飞书手机端可以直接访问moltbot的agent

7：ssh证书，域名，OSS等服务器配套设施都有

8：本地还有Jenkins自动化工具

想要完成的目标：


通过个人开发站点作为核心，打通开发环境，生产环境，移动端，实现全链路协同编程。具体思路：

一阶段：
（工作站有关）
1：能够实现本地的个人工作站，像monkey-code一样，能够为新需求开一个独立的容器，实现自由调度本地模型或者api模型，使用webUI可视化文件系统，终端，模型思考过程，可预览页面，以及自动丢弃之前的实例
2：可以选择是否保留这个实例，如果用户当前退出系统（或者手动关闭），则处于静默状态（或者基于快照），不再占用资源，这样用户下次还可以继续之前的编程继续推进
3：用户可以选择是否需要部署，通过部署有关skills，能够推送到远程仓库，并实现自动化部署流程，用户可以通过工作站得到当前是否部署成功，失败的话是什么原因（当然这个不可能很准确的体现，初期需要迭代）
4：会自动编写测试用例，并且通过mcp工具，提前获取到项目运行的问题，在让用户预览之前就解决出现的问题，直到完全不出现错误才会推送用户审查

（调度中心有关）
工作站在收到用户的指令以后，会自动推送到资源调度中心，调度中心专门会对问题进行分析，使用RAG和小模型（或者本地模型）的推断，找到最符合的内容，同步提供给大模型（比如用户可能提出需要重构一个前端项目，则会在技能列表找到适合的技能，同时数据库（不仅仅限于sqlite或者RAG，也可能是MongoDB）中找到所有有关的内容，并且统一交给模型分析，提炼出更适合大模型的prompt，再推给远端大模型，从而尽可能的减少token并提高准确度）。在大模型返回以后，将thinking过程推送给工作站，同步给出变更（这我不太清楚，可能需要文件系统的支持），就像MonkeyCode那样。

（RAG有关）
作为全局的上下文库，大模型本身（特别是不附带memories的远端模型），实际上maxtoken只有128k，而我希望构建一套多维向量体系，结合类似E:\enterprise-workspace\agent-orchestrator-platform的推荐系统，给出大模型足够多的参考，从而更能理解用户的需求。比如用户可能不太清楚自己需要改什么（假设需要改E:\enterprise-workspace\references\frontend-monorepo这个项目，但是很难全部打出来，而且即使通过文件夹选择，也不一定能够记得住），那么用户只需要使用NLP自然语言告知：我想改我之前写的一个monorepo前端项目，那么本地模型或者qwen小模型（在moltbot配置中我配置了很多小模型）是能够将NLP和RAG的内容匹配，并给用户尽可能推荐和提示的。当然这就需要每次用户在提供需求的时候，能够详细，完整的记录，从而使得RAG更多维度被填充。通过建立索引和向量提示，很轻松的推荐给用户可能的需求（比如：你是不是希望改进E:\enterprise-workspace\references\frontend-monorepo项目，之前你有修改过图标，并且已经保存了，现在你是不是想继续工作，还是想改一下共享组件）当然，这是基于多次迭代的结果，记录的内容维度需要有动态调整的过程，确保逐步贴近用户的需求（在这个过程中可能使用Ask Question工具提供选项询问用户一些推荐是否准确，从而方便后续的改进），就像很多购物网站的联想提示一样。

二阶段：
当一阶段全部完成以后，我希望有更为复杂的功能
（工作站有关）
此时工作站应该能够看到之前的提交记录，很多github skills有这个功能，虽然是自动提交的，但是对于release，main，feature，bugfix等分支，能够可视化的看到关键信息，当然这个可以使用专门的后台管理子系统体现

（移动端有关）
此时应该集成移动端，方便开发者能够随时给予指令调度工作台，并方便的查阅进度，可以使用已经集成好的飞书机器人，也可以基于
E:\enterprise-workspace\references\mobile-apps或者重新编写单独的移动端，移动端应该能够接受推送，通过webhook或者其他工具，从而使得全过程不受设备的限制

三阶段：
（工作站有关）
当一二阶段都非常完善以后，此时项目可以不再面向个人，可以考虑实现类似monkeyCode那种SaaS，这个时候需要引入严格的租户，角色等，并且参考阿里云等SaaS进行权限控制，不过这些暂时可以只是写入文档，预留位置就行。

（高维度扩展）
项目在长时间的运行以后，应该有自我进化的能力，由于已经积累了海量的参数，应该可以对用户未提出的内容做推荐，从而完善系统的设计和功能（比如提供更换头像，偏好设置等功能）